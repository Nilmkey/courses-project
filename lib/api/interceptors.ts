// ============================================================
// Глобальная обработка ошибок API
// ============================================================
// Импортируй этот файл в root layout для подключения интерсепторов
// ============================================================

import { addErrorObserver, addResponseInterceptor } from "./api-client";

// -------------------------------------------------------------
// 401 — редирект на логин
// -------------------------------------------------------------

addErrorObserver((error) => {
  if (error.status === 401) {
    console.log("[API Interceptor] 401 — пользователь не авторизован");
    // TODO: Добавить редирект когда будет нужен
    // redirect("/login");
  }
});

// -------------------------------------------------------------
// Логирование всех ответов (для отладки)
// -------------------------------------------------------------

addResponseInterceptor((response) => {
  // Можно включить для отладки
  // console.log('[API] Ответ:', response.config.url, response.data);
  return response;
});

// -------------------------------------------------------------
// Пустой компонент для импорта в layout
// -------------------------------------------------------------

export function ApiInterceptorsProvider() {
  return null;
}
